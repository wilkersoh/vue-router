import { createStore } from "vuex";
import counter from "./modules/counter";

const state = {
  //
};
const mutations = {
  //
};
const actions = {
  //
};

const getters = {
  //
};

export default createStore({
  modules: {
    counter,
  },
  state,
  mutations,
  actions,
  getters,
});
