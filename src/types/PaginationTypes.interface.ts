/*
1. console.log(theDataYouWantTurnToType in devtool)
2. right click the data, select "Store object as global"
3. command + p, > , paste JSON as Types
*/
export interface PaginationTypes {
  userId: number;
  id:     number;
  title:  string;
  body:   string;
}
